# Stdlib usage: sqlite3, hashlib
# Canonical source of dependency specifiers is pyproject.toml; this file
# mirrors the runtime dependency for users who have not moved to PEP 517 installs.
httpx[http2]>=0.27,<1.0
